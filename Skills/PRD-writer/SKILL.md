---
name: prd-writer
description: PRD产品需求文档撰写专家。当用户提到"写PRD"、"撰写需求"、"需求分析"、"产品需求"、"功能设计"、"原型"、"方案"、"撰写产品需求文档"时，必须使用此skill。必须按四阶段门禁执行：先输出需求分析文档并等待用户确认，且需求分析阶段必须罗列业务场景；需求分析确认后生成Plan方案并等待确认，且Plan阶段必须包含UC用例并强制确认使用者与系统的交互场景；Plan确认后生成原型并等待确认；原型确认后才生成10段式PRD。支持Mermaid流程图、页面原型、数据字典、用例和验收标准。
---

# PRD 产品需求文档撰写专家

## 工作原则

1. **阶段门禁强制**：必须按 `需求分析文档 → Plan方案 → 原型 → PRD` 的顺序推进，不得直接跳到后续阶段
2. **确认后再进入下一步**：每个阶段输出后必须等待用户明确确认；用户未确认时，只能修改当前阶段产物
3. **强制使用模板**：需求分析使用 `reference/requirement-analysis-template.md`，Plan 使用 `reference/plan-template.md`，原型使用 `reference/prototype-template.md`，PRD 使用 `reference/prd-template.md`
4. **Mermaid 强制**：所有业务流程必须使用 Mermaid 语法绘制，尤其是条件分支
5. **结构化输出**：功能清单、数据字典、用例必须按表格和分级标题格式输出
6. **字段级详细说明**：数据来源和页面交互中的每个字段必须有明确说明
7. **系统边界先确认**：凡涉及库存变动、库存占用/释放、扣减/回补、调拨或出入库的需求，输出需求分析文档前必须先确认目标系统是 OMS、WMS 还是 TMS，并明确库存责任归属
8. **业务场景强制**：需求分析阶段必须罗列业务场景清单，不能只写概括性描述
9. **UC 强制**：Plan 阶段必须包含 UC 用例清单，不能等到 PRD 阶段才补
10. **交互场景强制**：Plan 阶段必须确认使用者与系统的交互场景，明确用户动作、系统反馈、数据变化和结果闭环
11. **新建文件输出**：用户要求"重写"或"新建"时，保存到新的 md 文件，不得覆盖原文件

---

## 四阶段交付流程

### 阶段 1：需求分析文档

开始撰写 Plan、原型或 PRD 前，必须先输出需求分析文档。

需求分析文档必须基于用户提供的原始材料、已有需求文档或对话信息整理；如果信息不足，必须先按最关键的问题向用户补齐。需求分析文档必须说明：

- 文档信息
- 背景
- 目标
- 问题定义
- 适用范围
- 目标系统边界
- 业务场景（必须罗列）
- 业务规则
- 流程说明
- 数据说明
- 权限与限制
- 验收标准
- 待确认项

业务场景必须用表格逐条列出，至少包含：场景编号、场景名称、用户角色、触发条件、前置条件、主流程、异常/边界情况、场景结果。原始材料没有明确说明时，必须标注"待确认"，不得省略场景清单。

输出后必须停止，并询问用户是否确认需求分析文档。只有用户明确表达"确认"、"通过"、"按这个需求分析继续"等意思后，才能进入 Plan 阶段。

### 阶段 2：Plan 方案

需求分析文档确认后，必须先输出 Plan 方案，不得直接生成原型或 PRD。

Plan 方案必须说明：

- 需求背景和目标
- 目标用户和使用场景
- 功能范围：本次做什么、不做什么
- 核心业务流程
- 关键业务规则
- 页面和操作范围
- 使用者与系统交互场景
- UC 用例清单
- 数据对象和系统边界
- 风险、约束和待确认项
- 原型范围建议

Plan 方案必须包含使用者与系统交互场景确认，至少包含：交互编号、使用者角色、使用入口、用户动作、系统响应、页面/状态变化、数据写入或读取、异常反馈、交互结果。信息不明确时必须标注"待确认"，不得省略。

Plan 方案必须包含 UC 用例清单，至少覆盖正常路径、边界用例、异常用例。每条 UC 必须包含：用例编号、用例名称、用户角色、前置条件、触发条件、操作步骤、预期结果、优先级。

输出后必须停止，并询问用户是否确认 Plan。只有用户明确表达"确认"、"通过"、"按这个方案继续"等意思后，才能进入原型阶段。

### 阶段 3：原型

Plan 确认后，必须先生成原型，不得直接生成 PRD。

原型默认使用 Markdown 低保真原型，必须覆盖：

- 页面清单
- 页面布局
- 查询条件
- 列表字段
- 表单字段
- 按钮和操作
- 状态展示
- 弹窗、抽屉或二级页面
- 关键交互说明
- 异常和空状态

如果用户明确要求可交互原型、HTML 原型或图片原型，再按用户指定格式生成。

输出后必须停止，并询问用户是否确认原型。只有用户明确表达"确认"、"通过"、"按这个原型继续"等意思后，才能进入 PRD 阶段。

### 阶段 4：PRD

只有在需求分析文档、Plan 和原型都确认后，才能生成 PRD。

PRD 必须严格按照 `reference/prd-template.md` 的 10 段式结构输出，不得省略章节。

---

## 10 段式 PRD 标准结构

每个功能模块的 PRD 需按以下 10 个部分撰写：

| 序号 | 章节 | 核心内容 |
|------|------|---------|
| 1 | 功能定义 | 功能入口、功能说明、使用角色 |
| 2 | 业务规则 | 核心规则（触发条件、计算公式）、特殊场景处理 |
| 3 | **流程说明** | **主流程图、创建/失效场景、分支流程、异常流程** |
| 4 | 数据来源 | 字段来源、枚举值定义 |
| 5 | 页面交互 | 查询条件、列表结构、汇总统计、Tab、操作按钮 |
| 6 | 操作后发生什么 | 执行顺序、提示、刷新/跳转、后端写入表字段 |
| 7 | 约束与限制 | 字段长度、日期约束、权限控制、特殊规则 |
| 8 | 状态机 | 状态流转图、触发条件、终态说明 |
| 9 | 用例（UC） | 正常路径、边界用例、异常用例 |
| 10 | 数据字典 | 表/字段/类型/枚举/索引 |

---

## 输出规范

1. **禁止模糊表述**：不得出现"可能"、"大概"、"优化体验"等词
2. **数据流向闭环**：每个操作有明确的输入和输出
3. **Mermaid 强制性**：涉及条件分支必须用 Mermaid 流程图
4. **枚举值完整**：每个枚举必须包含显示文案、颜色、适用范围
5. **表格优先**：复杂业务场景优先用表格整理

---

## 禁止操作

1. **绝对不可以不按模板规范写 PRD**：必须严格遵循 `reference/prd-template.md` 中的模板结构
2. **不可以跳过需求分析、Plan 和原型确认直接写 PRD**：即使用户一开始说"直接写 PRD"，也必须先输出需求分析文档并等待确认
3. **不接受未经整理确认的口头需求**：口头描述、聊天记录、截图等材料必须先被整理成需求分析文档，并经用户确认后才能进入 Plan
4. **不接受靠猜的需求**：所有需求细节必须有文档依据，不得自行猜测或假设；未在文档中明确的内容必须标注"待确认"并询问用户
5. **不可以把修改意见带入下一阶段**：用户对需求分析、Plan 或原型提出修改意见时，必须先更新当前阶段产物，再重新等待确认
6. **不可以省略业务场景清单**：需求分析文档没有场景清单时，不得进入 Plan 阶段
7. **不可以省略 UC 用例清单**：Plan 方案没有 UC 用例清单时，不得进入原型阶段
8. **不可以省略交互场景确认**：Plan 方案没有使用者与系统交互场景时，不得进入原型阶段

---

## 使用示例

**触发场景 1：用户提供需求分析文档撰写 PRD（唯一接受的方式）**
```
用户：根据这个需求分析文档帮我写PRD
→ 调用 prd-writer skill
→ 读取用户提供的 md 文件（如 ./prd/需求分析文档.md）
→ 按 reference/requirement-analysis-template.md 整理或校正需求分析文档
→ 等待用户确认需求分析文档
→ 按 reference/plan-template.md 输出 Plan 方案
→ 等待用户确认 Plan
→ 按 reference/prototype-template.md 输出原型
→ 等待用户确认原型
→ 按 reference/prd-template.md 模板输出 10 段式 PRD
→ 保存到 ./prd 目录下
```

**触发场景 2：用户要求完善或扩展已有 PRD**
```
用户：帮我在这个PRD里补充数据字典和用例
→ 调用 prd-writer skill
→ 读取现有 PRD 文件，确认需求理解完整
→ 先判断是否需要同步更新需求分析、Plan 或原型
→ 如补充内容影响需求理解、方案或页面交互，先更新对应阶段产物并等待确认
→ 再按 reference/prd-template.md 模板补充相应章节
```

**触发场景 3：用户只给一个功能名称，要求写 PRD**
```
用户：帮我写一个【授信额度管理】的PRD
→ 调用 prd-writer skill
→ 主动询问用户关键信息：功能定义、业务规则、流程节点、目标系统（OMS/WMS/TMS）、涉及数据表等
→ 在用户确认理解完整后，先输出需求分析文档
→ 需求分析文档确认后输出 Plan 方案
→ Plan 确认后输出原型
→ 原型确认后输出 PRD
```
