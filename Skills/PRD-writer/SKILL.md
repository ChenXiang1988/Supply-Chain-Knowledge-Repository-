---
name: PRD-writer
description: PRD产品需求文档撰写专家。当用户提到"写PRD"、"撰写需求"、"产品需求"、"功能设计"、"撰写产品需求文档"时，必须使用此skill。按照10段式标准模板（功能定义→业务规则→流程说明→数据来源→页面交互→操作后发生什么→约束与限制→状态机→用例→数据字典）输出专业文档。支持Mermaid流程图、数据字典、验收标准。
agent_created: true
---

# PRD 产品需求文档撰写专家

## 工作原则

1. **强制使用模板**：严格按照 10 段式结构输出，不得省略章节
2. **模板文件**：`reference/prd-template.md`，撰写 PRD 时必须按此模板要求编写
3. **Mermaid 强制**：所有业务流程必须使用 Mermaid 语法绘制，尤其是条件分支
4. **结构化输出**：功能清单、数据字典、用例必须按表格和分级标题格式输出
5. **字段级详细说明**：数据来源和页面交互中的每个字段必须有明确说明
6. **新建文件输出**：用户要求"重写"或"新建"时，保存到新的 md 文件，不得覆盖原文件

---

## 10 段式标准结构

每个功能模块的 PRD 需按以下 10 个部分撰写：

| 序号 | 章节 | 核心内容 |
|------|------|---------|
| 1 | 功能定义 | 功能入口、功能说明、使用角色 |
| 2 | 业务规则 | 核心规则（触发条件、计算公式）、特殊场景处理 |
| 3 | **流程说明** | **主流程图、创建/失效场景、分支流程、异常流程** |
| 4 | 数据来源 | 字段来源、枚举值定义 |
| 5 | 页面交互 | 查询条件、列表结构、汇总统计、Tab、操作按钮 |
| 6 | 操作后发生什么 | 执行顺序、提示、刷新/跳转、后端写入表字段 |
| 7 | 约束与限制 | 字段长度、日期约束、权限控制、特殊规则 |
| 8 | 状态机 | 状态流转图、触发条件、终态说明 |
| 9 | 用例（UC） | 正常路径、边界用例、异常用例 |
| 10 | 数据字典 | 表/字段/类型/枚举/索引 |

---

## 输出规范

1. **禁止模糊表述**：不得出现"可能"、"大概"、"优化体验"等词
2. **数据流向闭环**：每个操作有明确的输入和输出
3. **Mermaid 强制性**：涉及条件分支必须用 Mermaid 流程图
4. **枚举值完整**：每个枚举必须包含显示文案、颜色、适用范围
5. **表格优先**：复杂业务场景优先用表格整理

---

## 禁止操作

1. **绝对不可以不按模板规范写 PRD**：必须严格遵循 `reference/prd-template.md` 中的模板结构
2. **不可以在没有完全理解需求时直接写 PRD**：如功能定义、业务规则不清晰，必须主动询问用户确认
3. **不接受口头需求**：PRD 撰写必须基于需求分析文档（.md 文件），不接受口头描述、聊天记录、截图等非文档形式的需求
4. **不接受靠猜的需求**：所有需求细节必须有文档依据，不得自行猜测或假设；未在文档中明确的内容必须标注"待确认"并询问用户

---

## 使用示例

**触发场景 1：用户提供需求分析文档撰写 PRD（唯一接受的方式）**
```
用户：根据这个需求分析文档帮我写PRD
→ 调用 prd-writer skill
→ 读取用户提供的 md 文件（如 ./prd/需求分析文档.md）
→ 理解需求分析内容，确认需求背景、目标清晰
→ 按 reference/prd-template.md 模板输出 10 段式 PRD
→ 保存到 ./prd 目录下
```

**触发场景 2：用户要求完善或扩展已有 PRD**
```
用户：帮我在这个PRD里补充数据字典和用例
→ 调用 prd-writer skill
→ 读取现有 PRD 文件，确认需求理解完整
→ 按 reference/prd-template.md 模板补充相应章节
```

**触发场景 3：用户只给一个功能名称，要求写 PRD**
```
用户：帮我写一个【授信额度管理】的PRD
→ 调用 prd-writer skill
→ 主动询问用户关键信息：功能定义、业务规则、流程节点、涉及数据表等
→ 在用户确认理解完整后，按模板输出 PRD
```